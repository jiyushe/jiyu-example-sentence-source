# 字节

<p><span class="chinese">8比特，即1个字节。</span><span class="english">So 8 bits, aka 1 byte.</span></p>

<p><span class="chinese">一个1字节的选择符域。</span><span class="english">A 1-byte selector field.</span></p>

<p><span class="chinese">两者总容量为8192字节。</span><span class="english">Total size is 8192 bytes.</span></p>

<p><span class="chinese">写入磁盘的字节数更少。</span><span class="english">Fewer bytes written to disk.</span></p>

<p><span class="chinese">使用面向字节的方式处理数据。</span><span class="english">Byte-oriented processing data.</span></p>

<p><span class="chinese">按字节显示文件的大小</span><span class="english">The size of the file in bytes.</span></p>

<p><span class="chinese">一个6字节长的终端系统标识符。</span><span class="english">A 6-byte end-system identifier.</span></p>

<p><span class="chinese">2009网上数据总量为281艾字节</span><span class="english">Exabytes of Online Data in 2009</span></p>

<p><span class="chinese">现在，我们破坏了里面的字节。</span><span class="english">Now--oh, now we've ruined them.</span></p>

<p><span class="chinese">这就是我们所说的一个字节。</span><span class="english">So this is what we know as a byte.</span></p>

<p><span class="chinese">所有的数据字节是发送MSB。</span><span class="english">All data bytes are transmitted MSB.</span></p>

<p><span class="chinese">请大家注意这个关键词“字节“</span><span class="english">Now the byte is the key word there.</span></p>

<p><span class="chinese">一个char型数据占据1字节或8比特。</span><span class="english">So a char size is 1 byte or 8 bits.</span></p>

<p><span class="chinese">使用千字节指定页面大小。</span><span class="english">Specify the page size in kilobytes.</span></p>

<p><span class="chinese">每个通用条目由12字节。</span><span class="english">Each IFD entry consists of 12 bytes.</span></p>

<p><span class="chinese">获取待执行的下一个字节码。</span><span class="english">Fetches the next bytecode to execute.</span></p>

<p><span class="chinese">元数据标记是一个四个字节的数字。</span><span class="english">A metadata token is a four-byte number.</span></p>

<p><span class="chinese">代码转换成多字节编码。</span><span class="english">Code conversion to multibyte encodings.</span></p>

<p><span class="chinese">这是多字节字符的一部分。</span><span class="english">This is a partial multi-byte character.</span></p>

<p><span class="chinese">使用文件描述符读取字节。</span><span class="english">Read the bytes with the file descriptor.</span></p>

