# daemon

Please help us improve the quality of example sentences! [Edit on Github](https://github.com/jiyushe/jiyu-example-sentence-source/blob/main/english/daemon.md)

<p><span class="chinese">我可以安装一个BC2代守护进程？</span><span class="english">Can I install a BC2 Daemon?</span></p>

<p><span class="chinese">非根端口监控程序对用户的攻击</span><span class="english">Attacks by a non-root daemon on a user</span></p>

<p><span class="chinese">如何运行hhvm在码头作为一个守护进程？</span><span class="english">How to run HHVM in Docker as a daemon?</span></p>

<p><span class="chinese">然后，这个守护进程就按照配置执行它们。</span><span class="english">The daemon then executes them, well, like clockwork.</span></p>

<p><span class="chinese">这个文件由管理服务器守护进程使用。</span><span class="english">This file is used by the administration server daemon.</span></p>

<p><span class="chinese">它是一个非常稳定的，并把特性封装的很好的IRC服务器守护程序。</span><span class="english">It is a highly stable and feature-packed IRC server daemon.</span></p>

<p><span class="chinese">启动后，它通常作为一个守护进程在后台运行。</span><span class="english">It normally runs as a daemon in the background when started.</span></p>

<p><span class="chinese">同时，守护进程在等待来自套接字的输入。</span><span class="english">Meanwhile, the daemon was waiting for input from the socket.</span></p>

<p><span class="chinese">这个命令在机器每次启动时启动守护进程。</span><span class="english">This command launches the daemon each time the machine restarts.</span></p>

<p><span class="chinese">可以把文件发送给守护进程，也可以向它请求文件。</span><span class="english">A transfer can send files to the daemon or request files from it.</span></p>

<p><span class="chinese">告诉守护进程下次同步是普通同步。</span><span class="english">Tell the daemon that the next HotSync should be a normal HotSync.</span></p>

<p><span class="chinese">现在，在目录中为每个服务守护进程创建一个文件。</span><span class="english">Now, within that directory, create a file for each service daemon.</span></p>

<p><span class="chinese">您可以预定义名称的范围或设置程序组范围的默认值。</span><span class="english">You can pre-define a range of names or set a daemon group-wide default.</span></p>

<p><span class="chinese">可以用此命令方便地在一组服务器上停止同一守护进程。</span><span class="english">The command is handy for stopping the same daemon on a collection of servers.</span></p>

<p><span class="chinese">执行预读和后写请求都需要用到该守护进程。</span><span class="english">This daemon is required to perform both read-ahead and write-behind requests.</span></p>

<p><span class="chinese">不能连接或启动守护程序。一些参数可能不正确。</span><span class="english">Unable to connect or launch daemon. Some values may be displayed incorrectly.</span></p>

<p><span class="chinese">在程序重新启动以后，您需要做的就是激活跟踪属性。</span><span class="english">After the daemon restart, all you need to do is activate the trace properties.</span></p>

<p><span class="chinese">如果有任何应用程序正在使用子系统，守护进程将不会终止。</span><span class="english">The daemon will not terminate if there are any applications using the subsystem.</span></p>

<p><span class="chinese">有两个UI搜索器和一个后端守护进程搜索器来搜索名为iso_deals的表。</span><span class="english">There are two UI searches and one backend daemon search on a table called iso_deals.</span></p>

<p><span class="chinese">如果你不能或就是不想托管一个你自己的守护进程，你可以使用我们所提供的。</span><span class="english">If you can't or just don't want to host a daemon on your own, you can use one of ours.</span></p>

