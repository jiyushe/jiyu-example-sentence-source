# ospf

Please help us improve the quality of example sentences! [Edit on Github](https://github.com/jiyushe/jiyu-example-sentence-source/blob/main/english/ospf.md)

<p><span class="chinese">本页将解释路由器如何在OSPF网路内通讯。</span><span class="english">This page will explain how routers communicate in an OSPF network.</span></p>

<p><span class="chinese">在OSPF区域的每个路由器有相同的链路状态资料库。</span><span class="english">Every router in the OSPF area will have the same link-state database.</span></p>

<p><span class="chinese">对哪些类型的网络的OSPF将选出一个备份指定路由器？</span><span class="english">On which types of network will OSPF elect a backup designated router?</span></p>

<p><span class="chinese">互动媒体活动中将教学生关于OSPF的术语。</span><span class="english">The Interactive Media Activity will teach students about OSPF terminology.</span></p>

<p><span class="chinese">OSPF中的开放的意思是它公开给公众且非专属的。</span><span class="english">The Open in OSPF means that it is open to the public and is non-proprietary.</span></p>

<p><span class="chinese">OSPF使用广播地址在广播网络链接上来路由泛洪。</span><span class="english">OSPF uses multicast addressing for route flooding on a broadcast network link.</span></p>

<p><span class="chinese">OSPF协议和工S一IS协议就是两个广泛使用的链路状态路由选择的例子。</span><span class="english">OSPF protocol and IS-IS protocol are two examples that used link-state routing.</span></p>

<p><span class="chinese">OSPF路由器通告它自己的链路状态和传递收到的链路状态。</span><span class="english">An OSPF router advertises its own link- states and passes on received link- states.</span></p>

<p><span class="chinese">使用迴路介面给所有执行OSPF的路由器被视为好的实务。</span><span class="english">It is considered good practice to use loopback interfaces for all routers running OSPF.</span></p>

<p><span class="chinese">OSPF从SPF树选择最快的无迴圈路径当做网路的最佳路径。</span><span class="english">OSPF selects the fastest loop- free path from the SPF tree as the best path in the network.</span></p>

<p><span class="chinese">下列哪一项正确描述了运行的OSPF的路由器的进程标识符的作用？</span><span class="english">Which of the following describe the process identifier that is used to run OSPF on a router?</span></p>

<p><span class="chinese">那些想要获得Cisco工作的求职者可能会认为在协议路径中并不包含情感，但是我向你保证，在面试的过程中肯定搀杂着情感的成分。</span><span class="english">Our Cisco job seeker above may think there is no emotion involved with OSPF routing but I assure you there is.</span></p>

<p><span class="chinese">我们必须正视，考试涵盖了许多单宁物质以网络为基础的OSPF路由器从一棒。</span><span class="english">Let's face it, the CCNA single exam covers a lot of material, from basic networking to OSPF to router on a stick.</span></p>

<p><span class="chinese">实作距离向量协定的路由器，比实作OSPF的路由器，需要较少记忆体和较不强的处理器。</span><span class="english">Routers that implement distance vector protocols need less memory and less powerful processors than those that implement OSPF.</span></p>

<p><span class="chinese">文章分析了OSPF协议加密措施存在的安全隐患，并对现有的加密方案进行了改进。</span><span class="english">In this paper, the hidden trouble in the encryption for OSPF protocol is analyzed and the existing encryption schemes improved.</span></p>

<p><span class="chinese">当OSPF被要求广播一迴路网路时，OSPF总是广播此具32位元遮罩的迴路主机路径。</span><span class="english">When OSPF is requested to advertise a loopback network, OSPF always advertises the loopback as a host route with a 32-bit mask.</span></p>

<p><span class="chinese">这表格可与OSPF所使用的相邻资料库相比较，EIGRP所支援的每个协定皆有一个相邻表。</span><span class="english">This table is comparable to the adjacency database used by OSPF. There is a neighbor table for each protocol that EIGRP supports.</span></p>

<p><span class="chinese">整个OSPF软件的实现是以公司开发了的路由器软、硬件作为基本平台。</span><span class="english">The implementation of the OSPF routing protocol software was based on the platform of the developed router's software and hardware.</span></p>

<p><span class="chinese">通过描述其主要任务、分组格式和数据流程，给出了一种基于开放最短路径优先OSPF的路由内算法。</span><span class="english">This paper describes the main task, packet format and data flow of IARP, and presents an intrazone routing algorithm based on OSPF.</span></p>

<p><span class="chinese">本文的最后对OSPF路由选择算法的进一步设计提出了设想，并对其应用前景及未来发展进行了展望。</span><span class="english">At last, the future development of the OSPF routing algorithms is pointed out. The future of it and its development are also discussed.</span></p>

