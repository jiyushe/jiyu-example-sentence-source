# initialization

<p><span class="chinese">推拉力机当机无法初始化?。</span><span class="english">Machine hangs-up on initialization?</span></p>

<p><span class="chinese">数组初始化的格式错误。</span><span class="english">An array initialization was ill-formed.</span></p>

<p><span class="chinese">检查驱动程序的初始状态。</span><span class="english">Check the initialization status of the driver.</span></p>

<p><span class="chinese">使用非常数的值来初始化一个变数。</span><span class="english">Initialization of a variable using a nonconstant value.</span></p>

<p><span class="chinese">该方法将作为初始化的一部分被调用。</span><span class="english">This method will be called as part of the initialization.</span></p>

<p><span class="chinese">第一个文件执行初始化以及设置任务。</span><span class="english">The first file performs initialization and any setup tasks.</span></p>

<p><span class="chinese">发布许可证存储模块的初始化失败。</span><span class="english">The Publishing License Storage module initialization failed.</span></p>

<p><span class="chinese">您将使用它运行任何扩展初始化代码。</span><span class="english">You would use this to run any extension initialization code.</span></p>

<p><span class="chinese">在运行时初始化期间填充这个字段。</span><span class="english">This field is populated during initialization of the runtime.</span></p>

<p><span class="chinese">初始化代码的最后一部分就是摄像机设置。</span><span class="english">The final part of the initialization code is the camera setup.</span></p>

<p><span class="chinese">注意该逻辑动作会在初始化时执行一次。</span><span class="english">Note that this logic could be performed once during initialization.</span></p>

<p><span class="chinese">平台调用这个类来执行初始化和清理工作。</span><span class="english">The platform calls this class to perform initialization and cleanup.</span></p>

<p><span class="chinese">卡片初始化由发卡机完成，主要包括设置射频卡密码、控制字及初始化卡号等。</span><span class="english">The initialization consists of Key A, Key B, Control byte and Card No.</span></p>

<p><span class="chinese">这个错误在类装入的初始化阶段抛出。</span><span class="english">This error is thrown during the initialization phase of class loading.</span></p>

<p><span class="chinese">因此，必须在该应用程序中进行创建和初始化。</span><span class="english">Thus, creation and initialization has to be done within this application.</span></p>

<p><span class="chinese">是到特定于应用程序数据的指针，用于程序的初始化。</span><span class="english">This is a pointer to application-specific data for program initialization.</span></p>

<p><span class="chinese">读取初始化文件后，将检查是否存在新的电子邮件。</span><span class="english">After reading the initialization file, the presence of new e-mail is checked.</span></p>

<p><span class="chinese">航迹起始是多目标航迹处理中的重要问题。</span><span class="english">Track initialization is an important problem in multi-target track processing.</span></p>

<p><span class="chinese">稍微增加了GDFS初始化错误操作码的冗长度。</span><span class="english">Slightly increased verbosity of GDFS access initialization error handling code.</span></p>

<p><span class="chinese">在每个例子中，有我们有一个分号来指明,那是我们的初始化。</span><span class="english">In every example thus far we've had a semicolon saying that's our initialization.</span></p>

