# instantiation

<p><span class="chinese">下图强调了继承与实例化的异同。</span><span class="english">A diagram emphasizes the orthogonality of inheritance and instantiation</span></p>

<p><span class="chinese">在程序运行时随需构造新的实例化类。</span><span class="english">Construct new instantiation classes on demand while the program is running.</span></p>

<p><span class="chinese">推迟实例化的另一个好处在于内存使用的优化。</span><span class="english">The other benefit to deferred instantiation is optimized memory utilization.</span></p>

<p><span class="chinese">此类在类实例化时构建一个键值对哈希表。</span><span class="english">This class builds a hash table of key-value pairs at the class instantiation.</span></p>

<p><span class="chinese">定制验证器需要在表单实例化的时候添加到此表单。</span><span class="english">Custom validators need to be added to the form at the time of form instantiation.</span></p>

<p><span class="chinese">结合部分实例化方法将子句搜索方法提升至一阶。</span><span class="english">The authors updated clause searching method to first-order by partial instantiation method.</span></p>

<p><span class="chinese">在复合应用程序的实例化过程中，社区角色被映射到域角色。</span><span class="english">During instantiation of a composite application, community roles are mapped to domain roles.</span></p>

<p><span class="chinese">并且终结还为可终结对象的实例化和垃圾收集带来了开销。</span><span class="english">And finalization adds overhead to instantiation and garbage collection of finalizable objects.</span></p>

<p><span class="chinese">实例支持，控制所请求服务实现的实例化处理</span><span class="english">Instance support, controlling instantiation of service's implementation for request processing.</span></p>

<p><span class="chinese">重置该实例。落空所有未处理惩罚的数据。这个在实例化对象时被隐含地调用。</span><span class="english">Reset the instance. Loses all unprocessed data. This is called implicitly at instantiation time.</span></p>

<p><span class="chinese">另一种方法是在程序运行时按需构造新的实例化类。</span><span class="english">Another approach is to construct new instantiation classes on demand while the program is running.</span></p>

<p><span class="chinese">这些映射可以并行进行维护，并且可以构建为类实例化过程中的一部分。</span><span class="english">The maps are maintained in parallel and are constructed as part of the class instantiation process.</span></p>

<p><span class="chinese">即使在对象更新它们状态的情况下，也应该谨慎地考虑对象实例化。</span><span class="english">Even in the case when objects update their state, object instantiation should be carefully considered.</span></p>

<p><span class="chinese">在这个模型的最简单的实例化过程中，您可以直接将代理侦听器连接到远程端点。</span><span class="english">In the simplest instantiation of this model, you directly wire the proxy listener to a remote endpoint.</span></p>

<p><span class="chinese">最后应用参数化设计技术实现了通用模板模块的实例化。</span><span class="english">Finally, parametric design technique is used to facilitate the instantiation of the general template-module.</span></p>

<p><span class="chinese">在应用程序运行时，连接点随时会有，比如对象的实例化和方法的调用。</span><span class="english">As an application runs, joinpoints fly by, such as the instantiation of objects and the invocation of methods.</span></p>

<p><span class="chinese">只要泛型类的运行时实例化没有包括所需的构造函数，就抛出异常。</span><span class="english">Throw an exception whenever a run-time instantiation of the generic class does not include a needed constructor.</span></p>

<p><span class="chinese">这些界限可以指定一组可用的构造函数，它们必须出现在参数的每一个实例化中。</span><span class="english">These bounds could specify the set of available constructors that must be present in any instantiation of the parameter.</span></p>

<p><span class="chinese">服务器虚拟化通过为快速实例化创建模板映像，提供了快速供给新环境的能力。</span><span class="english">Server virtualization provides the capability to rapidly provision new environments by creating template images for quick instantiation.</span></p>

<p><span class="chinese">这种实例化涉及到要调用的类的默认构造函数，随后还要注入依赖关系。</span><span class="english">This instantiation involves the default constructor for the class being called, with the injection of dependencies happening afterwards.</span></p>

