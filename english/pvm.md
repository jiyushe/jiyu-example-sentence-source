# pvm

<p><span class="chinese">除了其它关键的功能外，PVM允许支持多个标准。</span><span class="english">Among others key features, the PVM allows multiple standards support.</span></p>

<p><span class="chinese">鉴于PVM动态规模可扩展性的基础问题是任务透明迁移。</span><span class="english">PVM task migration is the basic issue of the PVM dynamic size scalability.</span></p>

<p><span class="chinese">因此，基于PVM构建一个流程语言，就是创建一套活动实现集，就这么简单。</span><span class="english">So building a process language on the PVM is as easy as creating a set of activity implementations.</span></p>

<p><span class="chinese">PVM拥有着大量的用户，现已成为世界上分布式计算事实上的标准。</span><span class="english">With tens of thousands of users, PVM has become the de facto standard for distributed computing world-wide.</span></p>

<p><span class="chinese">PVM技术是语言无关的，我认为这一点很棒，尤其是如果在今后几年里出现新的标准！</span><span class="english">The PVM technology is so language agnostic which I think is great, specially if new standards appear in the coming years!</span></p>

<p><span class="chinese">因此，有限元节点代码可以被并行化、在不同的基于PVM机群结点上运行。</span><span class="english">Therefore, the finite element code can be parallelized and executed on different cluster nodes on which PVM is available.</span></p>

<p><span class="chinese">因此，有限元节点代码可以被并行化、并能在基于不同的PVM机群结点上运行。</span><span class="english">Thus, nodal code of Finite Element Method can be parallelized and operated in the connecting point of different groups of PVM.</span></p>

<p><span class="chinese">该文对DADI-E的特点进行了阐述，包括RMA机制、动态进程管理和对PVM的支持。</span><span class="english">Features of DADI-E are presented in the paper, including RMA mechanism, dynamic process management and special support for PVM.</span></p>

<p><span class="chinese">此外，本文还选用通过商用网络连接起来的PC机，以及并行虚拟机PVM和分布式操作系统LINUX，共同构成了一个机群系统作为并行计算平台。</span><span class="english">Moreover, this paper constructs a parallel compute platform to apply the new algorithm by using PC group and PVM virtual system and LINUX OS.</span></p>

<p><span class="chinese">在并行矩阵乘上的对照实验表明，改进的PVM具有更短的响应时间。</span><span class="english">The experiment results on parallel matrix multiplication demonstrate that the response time of improved PVM is less than that of conventional PVM.</span></p>

<p><span class="chinese">PVM和MPI是广泛使用的并行编程环境，它们需要作为并行计算的基本构建而集成到元计算系统中去。</span><span class="english">PVM and MPI , which are popular parallel programming environments, will be integrated into the metacomputing system as the infrastructure of parallel computing.</span></p>

<p><span class="chinese">PVM的装配采用群型模式，程序结构采用SPMD模式，实现了基于PC机网络环境的并行计算。</span><span class="english">The implementation of PVM used cluster model and the structure of program used SPMD model, and a parallel computing environment based on PVM was constructed on cluster of PCs.</span></p>

