# executable

Please help us improve the quality of example sentences! [Edit on Github](https://github.com/jiyushe/jiyu-example-sentence-source/blob/main/english/executable.md)

<p><span class="chinese">只读和可执行的代码。</span><span class="english">Code that is read-only and executable.</span></p>

<p><span class="chinese">对个人机用户来说，压缩文件是可执行的。</span><span class="english">For PC users, the files are executable.</span></p>

<p><span class="chinese">但是什么是“可执行的架构”呢？</span><span class="english">But what is an "executable architecture"?</span></p>

<p><span class="chinese">启用关闭时运行定制的可执行文件。</span><span class="english">Enable to run a customized executable at shut-down.</span></p>

<p><span class="chinese">为每个可执行程序添加程序定义项</span><span class="english">Add a program definition entry for each executable program</span></p>

<p><span class="chinese">要添加到快捷栏您必须先选择一个可执行文件。</span><span class="english">You have to set an executable to add to the QuickBar first.</span></p>

<p><span class="chinese">简单来说，闭包就是指定的一段可执行代码。</span><span class="english">A closure is, quite simply, a named hunk of executable code.</span></p>

<p><span class="chinese">将从可执行代码中分离出来的UI模块打包。</span><span class="english">A1-1 Package UI modules separately from the executable code.</span></p>

<p><span class="chinese">必须提供可执行文件的完全限定路径。</span><span class="english">The fully-qualified path to the executable must be provided.</span></p>

<p><span class="chinese">建议我们不要将这些模块设置为可执行的。</span><span class="english">It is recommended that these modules not be set as executable.</span></p>

<p><span class="chinese">在软件世界中，真正的结果是可执行程序。</span><span class="english">In the world of software, real results are executable programs.</span></p>

<p><span class="chinese">可执行映射可以拥有一个或多个输入和输出卡。</span><span class="english">The executable map can have more than one input and output card.</span></p>

<p><span class="chinese">基于窗口的应用程序由一组DLL与可执行文件组成。</span><span class="english">Windows-based applications consist of executable files and DLLs.</span></p>

<p><span class="chinese">可执行过程精确定义了这个BPMS应该如何运转。</span><span class="english">An executable process defines exactly how the BPMS should behave.</span></p>

<p><span class="chinese">许多编程语言都有可执行单元这个概念。</span><span class="english">Most programming languages have the concept of an executable unit.</span></p>

<p><span class="chinese">所有此资料夹的可执行档都会在「指令稿」选单中出现。</span><span class="english">All executable files in this folder will appear in the Scripts menu.</span></p>

<p><span class="chinese">用以下命令把该程序编译成可执行程序</span><span class="english">This program can be compiled to an executable using the command line</span></p>

<p><span class="chinese">沃拉，您的ASP项目刚刚被出口到一个可执行文件。</span><span class="english">Walla , your ASP Project has just been exported to an executable file.</span></p>

<p><span class="chinese">所以可执行过程语言向系统解释了它该做什么。</span><span class="english">So an executable process language explains to a system what it should do.</span></p>

<p><span class="chinese">从一个分析图开始，然后将其转换成可执行的软件。</span><span class="english">Starting from an analysis diagram and turn that into executable software.</span></p>

