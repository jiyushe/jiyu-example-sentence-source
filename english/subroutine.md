# subroutine

Please help us improve the quality of example sentences! [Edit on Github](https://github.com/jiyushe/jiyu-example-sentence-source/blob/main/english/subroutine.md)

<p><span class="chinese">它的参数是一个方法名字和子函数引用。</span><span class="english">It takes a method name and a subroutine reference.</span></p>

<p><span class="chinese">这样可以跳过任何那个子路径中的代码。</span><span class="english">This would skip any remaining code in that subroutine.</span></p>

<p><span class="chinese">之后，控制权交给子函数和调用者。</span><span class="english">Control then passes both to the subroutine and the caller.</span></p>

<p><span class="chinese">让我们来看一看这个子程序并了解一下其工作原理。</span><span class="english">Let's take a look at the subroutine and describe how it works.</span></p>

<p><span class="chinese">在所有情况下最终的结果都是带有调用接口的子例程。</span><span class="english">The end result is, in all cases, a subroutine with a call interface.</span></p>

<p><span class="chinese">要集群化类似的规则，则需要在子例程中扫描规则。</span><span class="english">To cluster similar rules, start scanning the rules in the subroutine.</span></p>

<p><span class="chinese">然后使用以下子例程将字段权重装入内存。</span><span class="english">Then use the following subroutine to load the field weights into memory.</span></p>

<p><span class="chinese">当子例程被调用时，该例程正确地接收到了它的参数。</span><span class="english">When the subroutine is called, the routine receives its parameters correctly.</span></p>

<p><span class="chinese">子程序是在一段程序中可以被重用的部分，就像活字那样。</span><span class="english">A subroutine is a part of a program that can be re-used, just like movable type.</span></p>

<p><span class="chinese">一个子程序就是程序中可以被重用的部分，就像印刷术中的活字一样。</span><span class="english">A subroutine is ap art of a program th atc an be re-used, just like movable type.</span></p>

<p><span class="chinese">这种模式通常用于全局变量访问、分支以及子程序调用。</span><span class="english">This mode is often used for global variable access, branching, and subroutine calls.</span></p>

<p><span class="chinese">这个是显示的子程序，可以使用他在数码管上显示。</span><span class="english">This is an indication that the subroutine , he can use the digital control on the show.</span></p>

<p><span class="chinese">我们已经看到如何使用匿名的子例程或者对指定子例程的引用。</span><span class="english">We've already seen how to use an anonymous subroutine or a reference to a named subroutine.</span></p>

<p><span class="chinese">子程序的其余部分实现简单的震动检测算法。</span><span class="english">The remainder of the subroutine is the implementation of the simple shake-detection algorithm.</span></p>

<p><span class="chinese">介绍一种适用在EGA或VGA模式的生物电信号快速显示的子程序。</span><span class="english">A subroutine used for fast displaying of bioelectric signal in EGA or VGA is described in this paper.</span></p>

<p><span class="chinese">如果该字符完成了一个函数，编辑器允许函数折叠以最小化文本空间。</span><span class="english">If the character completes a function, the editor enables subroutine folding to minimize the text space.</span></p>

<p><span class="chinese">子函数允许任意长度的被加数，被加数和结果都是字符串，可以直接输出。</span><span class="english">Subroutine allow arbitrary length summand , summand, and the result is a string, you can output directly.</span></p>

<p><span class="chinese">例如strtok子例程是不可重入的，因为它保存着将被分隔为子串的字符串。</span><span class="english">For example, the strtok subroutine is not reentrant, because it holds the string to be broken into tokens.</span></p>

<p><span class="chinese">如果您确信在子例程被调用时某变量应该有特定的值，那么就检查一下情况是否如此。</span><span class="english">If you believe that a certain variable should have a specific value when a subroutine is called, check it.</span></p>

<p><span class="chinese">其中包括主程序、机器人动作控制子程序、语音播放子程序及语音识别程序。</span><span class="english">Including the main program, sub-control robot moves, voice and speech recognition to play subroutine program.</span></p>

