# http

<p><span class="chinese">Sinatra是直接映射到HTTP的。</span><span class="english">Sinatra is a direct mapping to HTTP.</span></p>

<p><span class="chinese">方法信息符合标准HTTP动词。</span><span class="english">Method information goes into the standard HTTP verbs.</span></p>

<p><span class="chinese">集群前端是一个软件HTTP负载均衡器。</span><span class="english">The cluster is fronted by a software HTTP load balancer.</span></p>

<p><span class="chinese">最简单的一种就是用类似HTTP的双向传输。</span><span class="english">The easiest way is to use a two-way transport like HTTP.</span></p>

<p><span class="chinese">HTTP是无状态请求/回复应用协议。</span><span class="english">HTTP is a stateless request-response application protocol.</span></p>

<p><span class="chinese">它迫使重新通过HTTPS发送不安全的HTTP请求。</span><span class="english">It forces an unsecured HTTP request to be re-sent over HTTPS.</span></p>

<p><span class="chinese">当进行HTTP代理时通过认证的使用者名称。</span><span class="english">User name to pass as authentication when doing HTTP proxying.</span></p>

<p><span class="chinese">谈到如何测试连通性，如HTTP请求和SOAP连接</span><span class="english">On testing connectivity such as HTTP request and SOAP connection</span></p>

<p><span class="chinese">只要有HTTP栈，在任何平台上都可以很轻松地消费数据服务</span><span class="english">Any platform with an HTTP stack can easily consume a data service</span></p>

<p><span class="chinese">用户代理程序从服务器的HTTP头信息中接收MIME类型。</span><span class="english">User agents receive the MIME type from HTTP headers set on the server.</span></p>

<p><span class="chinese">减少页面的HTTP请求数量只是万里长征第一步。</span><span class="english">Reducing the number of HTTP requests in your page is the place to start.</span></p>

<p><span class="chinese">关于HTTP报头及其取值的更多信息参见RFC2068。</span><span class="english">For more information about HTTP headers and their values refer to RFC 2068.</span></p>

<p><span class="chinese">所有的签名信息都存储在HTTP请求或响应头中。</span><span class="english">Have all signature information be stored within HTTP request or response headers.</span></p>

<p><span class="chinese">在HTTP头信息里加入缓存控制过期和最大存活时间标记。</span><span class="english">Add Expires and max-age directive of Cache-Control HTTP Header in server responses.</span></p>

<p><span class="chinese">如果存在未关闭的HTTP连接，虚拟用户脚本占用了18M的内存</span><span class="english">Virtual User script footprint of around 18MB if there are unclosed HTTP connections</span></p>

<p><span class="chinese">使用此科技的网页将拥有以HTTPS而非HTTP启动的全球资源定位器。</span><span class="english">Page utilising this technology will have URLs that start with HTTPS instead of HTTP.</span></p>

<p><span class="chinese">服务的客户可以通过任何可支持的传输器，比如HTTP或JMS来发送请求。</span><span class="english">Service consumers can send requests via any supported transport such as HTTP or JMS.</span></p>

<p><span class="chinese">你有没有发现对HTTP里的认证和防篡改机制有了越来越多的需求？</span><span class="english">Do you see the need for augmented authentication and tamper proof mechanisms in HTTP?</span></p>

<p><span class="chinese">现在的客户端以HTTP页眉的格式发送大量冗余数据。</span><span class="english">Clients today send a significant amount of redundant data in the form of HTTP headers.</span></p>

<p><span class="chinese">引入了对异步HTTP请求和响应的标准化支持。</span><span class="english">The introduction of standardised support for asynchronous HTTP requests and responses.</span></p>

