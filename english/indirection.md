# indirection

<p><span class="chinese">它在设计中引入了另一种间接方法。</span><span class="english">It introduces another indirection in the design.</span></p>

<p><span class="chinese">所以动态语言有效地添加了一级间接寻址。</span><span class="english">So dynamic languages effectively add a level of indirection.</span></p>

<p><span class="chinese">当访问对象时，它引入了一个间接的层。</span><span class="english">It introduces a level of indirection when accessing an object.</span></p>

<p><span class="chinese">链接是一种间接机制，客户端可以用它来在运行时发现URI。</span><span class="english">A link is an indirection that a client can use to discover URIs at runtime.</span></p>

<p><span class="chinese">如果查询是非递归的，则仅处理一个间接层次。</span><span class="english">If the query is non-recursive, only one level of indirection will be processed.</span></p>

<p><span class="chinese">把消息的创建者与处理者分开在一定程度上是间接的。</span><span class="english">There is a level of indirection that separates the creator and processor of the message.</span></p>

<p><span class="chinese">晚期绑定需求通常要求应用程序中存在一个间接寻址级别。</span><span class="english">A requirement for late binding generally requires a level of indirection in the application.</span></p>

<p><span class="chinese">我去掉了大量间接的内容和比较深奥的分类链接表示方法。</span><span class="english">I've snipped a lot of the indirection and the more esoteric ways taxonomy links are expressed.</span></p>

<p><span class="chinese">包装按值封送对象引用，从而使它们可以通过间接寻址返回。</span><span class="english">Wraps marshal-by-value object references, allowing them to be returned through an indirection.</span></p>

<p><span class="chinese">目的添加了一个间接层来支持复杂组件的重用和替换。</span><span class="english">Intents add a layer of indirection that enables sophisticated component reuse and substitution.</span></p>

<p><span class="chinese">这意味着XPDL有一个单独的间接层来为所谓的‘应用’服务。</span><span class="english">This means that XPDL has a separate level of indirection introduced to address so-called 'applications'.</span></p>

<p><span class="chinese">表空间在数据库与存储在数据库中的表之间提供了一个间接层。</span><span class="english">A tablespace provides a level of indirection between a database and the tables stored within the database.</span></p>

<p><span class="chinese">要拦截这些类的加载，您需要在程序的执行过程中进行某种程度的重定向。</span><span class="english">Intercepting the loading of these classes requires a level of indirection in the execution of the program.</span></p>

<p><span class="chinese">这个方法的优点在于间接层带来了巨大的部署弹性。</span><span class="english">The advantage of this approach is that the level of indirection provides for greater deployment flexibility.</span></p>

<p><span class="chinese">当主机改变了在网络中的位置或重新编号之后，这种间接的做法允许主机保留其名称。</span><span class="english">This indirection allows the names to remain stable when hosts are moved around in the network and renumbered.</span></p>

<p><span class="chinese">所以，为了避免一些繁琐的重复代码，本库定义了一些谓词用于在比较前应用间接性。</span><span class="english">So to avoid some tedious boiler-plate code this library defines predicates that apply an indirection before comparing.</span></p>

<p><span class="chinese">库伯勒-罗斯却将这种不直截了当的做法视作是一种懦弱的行为，认为这违背了医生应当对病人履行的基本的人道主义。</span><span class="english">Kübler-Ross saw this indirection as a form of cowardice that ran counter to the basic humanity a doctor owed his patients.</span></p>

<p><span class="chinese">电压互感器二次压降是一个变化量，且具有单向性和一定的随机性。</span><span class="english">The PT secondary loop voltage drop is a changing amount and has the characteristics of indirection and certain randomness.</span></p>

<p><span class="chinese">Schachter写道，“最严重的问题是，缩写服务在已经不堪重负的系统里又增加了一个间接层。”</span><span class="english">Schachter writes, “The worst problem is that shortening services add another layer of indirection to an already creaky system.”</span></p>

<p><span class="chinese">戴震以其自得的义理对传统理学进行了无情地揭露和批判，并转向对社会现实的批判。</span><span class="english">Dai Zhen, criticized the traditional Li Xue with his new Yi Li and changed his indirection of the criticism to the social reality.</span></p>

