# modulo

<p><span class="chinese">模数的范围必须为正。</span><span class="english">Modulo range must be positive.</span></p>

<p><span class="chinese">单位群的阶为2pq的模n剩余类环。</span><span class="english">Group of units of order 2pq of modulo n residue class ring.</span></p>

<p><span class="chinese">模调度是一类被广泛采用的软件流水调度算法。</span><span class="english">Modulo scheduling is a kind of widely used scheduling technique.</span></p>

<p><span class="chinese">方法返回的哈希代码执行求模操作。</span><span class="english">Class to perform a modulo operation on the hash code returned by the.</span></p>

<p><span class="chinese">通常，哈希函数包含对数组容量的取模操作。</span><span class="english">Often the hash function involves using the modulo operator with the table size.</span></p>

<p><span class="chinese">这是类似于模运算符，但可以使用浮点数。</span><span class="english">This is similar to the modulo operator but it works with floating point numbers.</span></p>

<p><span class="chinese">仅在你需要模算术或需要很大很大的数字时，才使用无符号类型。</span><span class="english">Use unsigned types only when you need modulo arithmetic or very very large numbers.</span></p>

<p><span class="chinese">模调度是一类重要的软件流水调度算法。</span><span class="english">Modulo scheduling is a state-of-art scheme in the literature of software pipelining.</span></p>

<p><span class="chinese">嵌入水印时，先对水印信息进行扩频，再利用基于模运算的方法嵌入水印。</span><span class="english">Spread spectrum was conducted first, and then it was embedded into watermark by means of integer modulo.</span></p>

<p><span class="chinese">这技术以柜台为模需要一个变数，另外的一个柜台为选择，和逻辑对于州控制。</span><span class="english">This technique requires a variable modulo counter, another counter for the selection, and logic for state control.</span></p>

<p><span class="chinese">在第四章中，考虑在两种二维距离之下有限域上线性码的几个问题。</span><span class="english">In Chapter 4, several problems are involved in linear codes over finite fields with two dimensional modulo metrics.</span></p>

<p><span class="chinese">协议中只含模加与模乘运算，运算简单、快速。</span><span class="english">The operation in protocol was simple and fast because only modulo addition and modulo multiplication were involved.</span></p>

<p><span class="chinese">多尺度边缘检测等价于寻找图像的小波变换模局部极值。</span><span class="english">Multiscale boundary detection is equivalent to searching out the local extremum of wavelet transform modulo of image.</span></p>

<p><span class="chinese">文章基于模合数二次剩余问题，提出了一种改进的方案，克服了W。</span><span class="english">Hence, an improved scheme based on square root modulo composites is proposed and the security drawback in W·Z·L scheme is overcome.</span></p>

<p><span class="chinese">在另一方面，本文致力于研究如何改进传统的基于线性偏差的地址交织策略来解决其存储器冗余和无法支持模寻址的问题。</span><span class="english">On the other hand, we are aimed to remove the memory redundancy of the previous linear skewing interleave scheme and try to support modulo addressing.</span></p>

<p><span class="chinese">保守的相关性分析可能会引入较多的模糊相关，这阻碍了模调度生成高效的调度结果。</span><span class="english">Conservative alias analysis may introduce many non-definite dependencies, which prevents modulo scheduling from finding a more efficient scheduling result.</span></p>

<p><span class="chinese">最后，通过与其它模加法器在结构以及算法等方面进行分析比较，表明本文所设计的模加法器性能优异。</span><span class="english">Finally, through the analysis and comparison with other kind of modulo adders, we draw the conclusion that the performance of the new modulo adder is better.</span></p>

<p><span class="chinese">多项式模归约算法是计算机代数中的基本问题之一，在编码算法和密码体制设计中有着广泛应用。</span><span class="english">Polynomial modulo reduction algorithms are one of the fundamental issues of computer algebra, and widely used in coding algorithms and cryptographic system design.</span></p>

<p><span class="chinese">研究一个关于n原根的数论函数的渐近性质，给出了一个较为精确的渐近公式。</span><span class="english">In this paper, we have studied the asymptotic formula of a number theoretic function about the primitive roots modulo N, and given a more precise asymptotic formula.</span></p>

<p><span class="chinese">分析了圆柱形波导气体激光器中的耦合损耗，给出了最低阶波导模EH11的耦合损耗与反射镜位置、曲率半径的函数关系。</span><span class="english">This paper analyses the coupling wastage in columniform wave guide gas laser, and gives the function between the coupling wastage of laser guide modulo EH11 and the position and radius of reflector.</span></p>

