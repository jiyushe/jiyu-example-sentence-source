# compiler

<p><span class="chinese">我们需要一个编译器。</span><span class="english">Okay, so a compiler.</span></p>

<p><span class="chinese">编译器截断多余位。</span><span class="english">The compiler truncates the extra bits.</span></p>

<p><span class="chinese">编译器或汇编器的输出。</span><span class="english">The output of a compiler or assembler.</span></p>

<p><span class="chinese">自动向量化的汇编器选项</span><span class="english">Compiler options for auto-vectorization</span></p>

<p><span class="chinese">用C语言开发简易编译系统。</span><span class="english">Used C language to develop simple compiler.</span></p>

<p><span class="chinese">在编译器层次处理可重用性</span><span class="english">Dealing with reentrancy at the compiler level</span></p>

<p><span class="chinese">使用这种文字编译器似乎可能有些古怪。</span><span class="english">This use of the word compiler might seem odd.</span></p>

<p><span class="chinese">用你的编译器完成简单的优化。</span><span class="english">Let your compiler do the simple optimizations.</span></p>

<p><span class="chinese">编译器将创建一个包含MSIL的文件。</span><span class="english">The compiler creates an file that contain MSIL.</span></p>

<p><span class="chinese">但是，编译器却发现一个函数。</span><span class="english">However, the compiler finds a function, instead.</span></p>

<p><span class="chinese">指定REBOL程序作为编译器。</span><span class="english">Specify the REBOL program rather than a compiler.</span></p>

<p><span class="chinese">我有一个可以运行的编译器，没人愿意碰它。</span><span class="english">I had a running compiler and nobody would touch it.</span></p>

<p><span class="chinese">编译器会进行截断资料的转换。</span><span class="english">The compiler performs the cast, which truncates data.</span></p>

<p><span class="chinese">讨论了在编译器层次上对可重入性的处理。</span><span class="english">Discusses dealing with reentrancy at the compiler level</span></p>

<p><span class="chinese">这些系统程序当中有一种叫做编译程序。</span><span class="english">What's the difference between an assembler and compiler?</span></p>

<p><span class="chinese">内置了一个内部资源编译器和反编译器。</span><span class="english">A built-in internal resources and anti-compiler compiler.</span></p>

<p><span class="chinese">编译器把代码翻译成中间代码。</span><span class="english">A compiler translates the code into an intermediary form.</span></p>

<p><span class="chinese">编译器在其中放置编译输出的目录。</span><span class="english">The directory where the compiler puts the compiled output.</span></p>

<p><span class="chinese">这告诉编译器生成位置无关代码。</span><span class="english">This tells the compiler to generate position-independent code.</span></p>

<p><span class="chinese">这样做会使编译器执行引数型别检查。</span><span class="english">Doing so causes the compiler to perform argument type checking.</span></p>

