# tcp

<p><span class="chinese">这些组件还使用了TCP和基于SIP的协议。</span><span class="english">TCP and SIP-based protocols are also used by components.</span></p>

<p><span class="chinese">传输控制协议为数据在主人之间的移动商议期间。</span><span class="english">TCP negotiates a session for data transfer between hosts.</span></p>

<p><span class="chinese">超出个时间，TCP会发出一个故障信号，然后放弃重传。</span><span class="english">TCP could signal a failure and give up when both end-points are fine.</span></p>

<p><span class="chinese">他招募了那家工厂的一些工人，开始为TCP生产螺旋形灯泡。</span><span class="english">He recruited some of the workers to start making spiral bulbs for TCP.</span></p>

<p><span class="chinese">另外ECAM以较低的代价实现了降低丢包率和TCP友好。</span><span class="english">In addition ECAM reduce the packet loss rate and achieve TCP friendly.</span></p>

<p><span class="chinese">首先，TCP确保接受方处于准备接收的状态。</span><span class="english">First, TCP ensures that the receiving computer is ready to accept data.</span></p>

<p><span class="chinese">TCP使用确认和重传即时来检测和修复错误。</span><span class="english">TCP uses acknowledgements and retransmission to detect and repair loss.</span></p>

<p><span class="chinese">该发现有助于建立光突发交换网络下的TCP吞吐量模型。</span><span class="english">The result is useful when building TCP throughput models over OBS networks.</span></p>

<p><span class="chinese">可以与TCP之间实现网络资源的公平竞争。</span><span class="english">AIMD flow and TCP flow can fairly compete network resources with each other.</span></p>

<p><span class="chinese">如果某个灯泡还没达到预计的10,000小时寿命就坏掉了，TCP会负责更换。</span><span class="english">When a bulb burns out before its projected 10,000-hour life, TCP replaces it.</span></p>

<p><span class="chinese">此外，TCP协议可以自动地丢弃重复的包，以及在接收方按照正确的顺序对包进行重新排列。</span><span class="english">TCP drops duplicate packets and rearranges packets that arrive out of sequence.</span></p>

<p><span class="chinese">未发现有任何TCP相析出，这与用相分计算预测的结果一致。</span><span class="english">No TCP phase was detected, which is consistent with predicted result by phacomp.</span></p>

<p><span class="chinese">描述OSI和TCP模型下协议的用途和基本操作</span><span class="english">Describe the purpose and basic operation of the protocols in the OSI and TCP models</span></p>

<p><span class="chinese">仿真实验表明该FACC可有效地工作，具有较好的TCP公平性和稳定的发送速率。</span><span class="english">Experiments show that FACC can provide steady sending rate and TCP friend fairness.</span></p>

<p><span class="chinese">对基于流体流量的TCP拥塞模型进行小信号稳定性分析。</span><span class="english">The small-signal stability analysis of fluid-flow congestion TCP model is discussed.</span></p>

<p><span class="chinese">一旦这个直接的TCP连接被建立，这个不可越的链路就能被关闭。</span><span class="english">Once the direct TCP connection is established, the out-of-band channel can be closed.</span></p>

<p><span class="chinese">该协议通过为反向ACK数据包提供资源预留以提高TCP性能。</span><span class="english">The Bi-RSVP provides resource reservation for ACK packets to improve TCP performance.</span></p>

<p><span class="chinese">文中介绍了TCP报文的封装情况、TCP报文段格式规定和TCP连接中的“三次握手”协议。</span><span class="english">The specification of TCP as well as its three way handshake is introduced in the paper.</span></p>

<p><span class="chinese">在消费者寻求传统白炽灯泡的替代品之时，TCP处于有利位置。</span><span class="english">TCP is well-positioned as consumers seek alternatives to traditional incandescent bulbs.</span></p>

<p><span class="chinese">第五部份分析了端口预言机制，第六部份分析了对等网的TCP建立。</span><span class="english">Section 5 analyzes port-prediction and Section 6 analyzes peer-to-peer TCP establishment.</span></p>

