# sql

Please help us improve the quality of example sentences! [Edit on Github](https://github.com/jiyushe/jiyu-example-sentence-source/blob/main/english/sql.md)

<p><span class="chinese">支持所有常用的SQL数据类型。</span><span class="english">Support for all common SQL data-types.</span></p>

<p><span class="chinese">运行安装程序，上面有删除选项。</span><span class="english">Run setup. exe and select remove SQL Server.</span></p>

<p><span class="chinese">借助于JDO或者JPA使用SQL、sql4o查询db4o</span><span class="english">Querying db4o using SQL via JDO or JPA, using sql4o</span></p>

<p><span class="chinese">每条SQL语句处理表的记录数？</span><span class="english">Number of processed table lines after SQL statements.</span></p>

<p><span class="chinese">正是这种享乐至上、田园式生活给了SQL灵感。</span><span class="english">This kind of enjoyment for most and arcadian life inspire SQL.</span></p>

<p><span class="chinese">在SQL语句的WHERE子句里没有指定键值</span><span class="english">Not specifying key values in the WHERE clause of an SQL statement.</span></p>

<p><span class="chinese">只需要一张表，一条多态查询生成的SQL大概是这样的</span><span class="english">Exactly one table is required. A polymorphic query generates SQL like</span></p>

<p><span class="chinese">学习SQL能够教会你以集合而非迭代的方式思考。</span><span class="english">Learning SQL will teach you to think in sets rather than iteratively.</span></p>

<p><span class="chinese">为以下应用储存数据的SQL速成版数据库</span><span class="english">SQL Express database, which stores data for the following applications</span></p>

<p><span class="chinese">涉及三张或更多的表，多态查询生成的SQL是这样的</span><span class="english">Three or more tables are involved. A polymorphic query generates SQL like</span></p>

<p><span class="chinese">属性数据存储在各项目组独自创建的SQL数据库中。</span><span class="english">Attribute datas Storage in SQL DataBase which were found by each item-group.</span></p>

<p><span class="chinese">SQL片段可以是任意复杂的，甚至可包含子查询。</span><span class="english">The SQL fragment can be as complex as you want avec even include subselects.</span></p>

<p><span class="chinese">可压缩，因为文本文件，如SQL或配置文件压缩率很高</span><span class="english">compressible, since text files such as SQL or properties are highly redundant</span></p>

<p><span class="chinese">介绍了SQL注入式攻击的原理及方法。</span><span class="english">This text has introduced the principle and method that SQL pouring-in attacks.</span></p>

<p><span class="chinese">这个总行数不包括子查询处理的行数。</span><span class="english">This total does not include rows processed by subqueries of the SQL statement.</span></p>

<p><span class="chinese">对于大多数.NET开发者来说，唯一一种真正的REPL式的代码就是SQL。</span><span class="english">For most .NET programmers, the only true REPL style coding you usually find is SQL.</span></p>

<p><span class="chinese">SQL语言是用户和应用程序与关系数据库的标准接口。</span><span class="english">SQL is the standar user and application program interface to a relational database.</span></p>

<p><span class="chinese">调整高负载的SQL语句，具体点击后面的英文链接。</span><span class="english">Tune high-load SQL statements, as described in Chapter 10, "Tuning SQL Statements".</span></p>

<p><span class="chinese">如果你是一个受虐狂，你能配合到一个SQL数据库，并记录所有的越轨行为。</span><span class="english">If you are a masochist, you can tie it to a SQL database and log all transgressions.</span></p>

<p><span class="chinese">从CodePlex下载的项目源码中还包含了创建这个数据库的SQL脚本。</span><span class="english">The project download on CodePlex includes the SQL script for creating this database.</span></p>

