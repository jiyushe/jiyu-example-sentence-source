# udp

<p><span class="chinese">UDP没有根据检查数侦测错误。</span><span class="english">UDP does detect errored datagrams with a checksum.</span></p>

<p><span class="chinese">UDP为程序发送数据报。</span><span class="english">UDP sends and receives datagrams for applications.</span></p>

<p><span class="chinese">RTMFP因为采用了UDP也提升了传送的速度。</span><span class="english">RTMFP also increases the quality of delivery through the use of UDP.</span></p>

<p><span class="chinese">TURN协议是一种穿越对称NAT的技术。</span><span class="english">TURN protocol is a technique for simple traversal of UDP through NAT.</span></p>

<p><span class="chinese">是一个无连接的UDP协议，因此，它可以很容易地欺骗。</span><span class="english">UDP is a connectionless protocol and, as such, it can be easily spoofed.</span></p>

<p><span class="chinese">相似地，许多不同的程序可以任何时间调用TDP或UDP。</span><span class="english">Similarly, many different applications can be using TDP or UDP at any one time.</span></p>

<p><span class="chinese">采用时间驱动的工作方式，并且选择UDP协议来实现系统软件。</span><span class="english">UDP protocol and time-drive method are adopted to realize the telecontrol system.</span></p>

<p><span class="chinese">因为UDP非连接导向且不可靠，SNMP可能会遗失讯息。</span><span class="english">Since UDP is connectionless and unreliable, it is possible for SNMP to lose messages.</span></p>

<p><span class="chinese">主控制端与代理服务器的通信采用UDP协议，作者定义了相关的命令格式编码。</span><span class="english">The communication protocol between the master control computer and the proxy server is UDP.</span></p>

<p><span class="chinese">它极大简化了网络编程，如TCP和UDP套接字服务器。</span><span class="english">It greatly simplifies and streamlines network programming such as TCP and UDP socket server.</span></p>

<p><span class="chinese">UDP提供在OSI模型的第4层的非连接导向、不确保封包正确地传送到目的地。</span><span class="english">UDP provides connectionless , unreliable transmission of packets at Layer 4 of the OSI model.</span></p>

<p><span class="chinese">这样改动之后，我们就可以部署UDP同时后端性能不打折。</span><span class="english">With this change, we were able to deploy UDP without compromising performance on the backend.</span></p>

<p><span class="chinese">UDP不使用视窗或ACK，所以应用层协定必须提供错误侦测。</span><span class="english">UDP does not use windowing or ACKs so application layer protocols must provide error detection.</span></p>

<p><span class="chinese">TDP和UDP是两个主导的传输层协议。都使用网络层的IP协议。</span><span class="english">TDP and UDP are the two predominant transport layer protocols. Both use IP as the network layer.</span></p>

<p><span class="chinese">如果你已经知道如何解读UDP包的头，不妨从第三章开始。</span><span class="english">For those of you who already know how to read a UDP packet header, chapter 3 is your starting point.</span></p>

<p><span class="chinese">因此，TCP不适合传输实时数据流，大多采用UDP来传输。</span><span class="english">Therefore, the real-time data stream transmission is not suitable for using TCP but mostly uses UDP.</span></p>

<p><span class="chinese">对于TCP的连接其结果接受，这是为UDP时生成一个UDP数据包接收。</span><span class="english">For TCP its the result of connection accept, and for UDP this is generated when a UDP packet is received.</span></p>

<p><span class="chinese">因此，那些好像在SKYPE使用的文件传输模式的程序使用不恰当的协议类似于UDP。</span><span class="english">Consequently applications such as the file-transfer module in Skype use contraindicated protocols like UDP.</span></p>

<p><span class="chinese">UDP关联请求被用在建立一个UDP传递过程中的关联来传递UDP数据包。</span><span class="english">The UDP ASSOCIATE request is used to establish an association within the UDP relay process to handle UDP datagrams.</span></p>

<p><span class="chinese">此外，在理论上，柜台模式应该可以处理这个UDP加密存在没有自每包。</span><span class="english">Moreover, in theory, counter mode should be able to handle the UDP encryption problem without a rekey for every packet.</span></p>

